/**
 * Example nodes file.
 * Rename this file to nodes.ts and fill the values.
 * Fill the values with your own values here.
 */
import type { LavalinkNodeOptions } from "lavalink-client";

export const nodes: LavalinkNodeOptions[] = [
	{
		id: "xxxxx",
		host: "example.com",
		port: 2333,
		authorization: "xxxxx",
		secure: false,
	},

	// add more nodes here
];
